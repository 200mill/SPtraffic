# SPtraffic 개발 계획

전국 시외/고속버스 및 간선철도 시간표 조회 시스템

---

## 1. 목표 및 범위

### 핵심 목표
- data.go.kr 공공 API를 통해 전국 시외/고속버스 및 KTX/일반열차 시간표를 수집·저장
- OpenStreetMap 기반 지도 위에 경로와 정류장을 시각화
- 출발지·도착지 입력으로 직행 및 환승 경로를 검색하고 시간표를 제공

### 대상 교통수단
- 시외버스 (intercity bus)
- 고속버스 (express bus)
- 간선철도: KTX, ITX-새마을, 무궁화, ITX-청춘 등

---

## 2. 기술 스택 (안)

| 영역 | 선택지 | 비고 |
|------|--------|------|
| 백엔드 언어 | Go | 경량 서버, 스케줄러 구현에 적합 |
| 웹 프레임워크 | Gin 또는 Echo | REST API 제공 |
| DB | PostgreSQL + PostGIS | 정류장 좌표, 노선 공간 데이터 처리 |
| 캐시/큐 | Redis | API 폴링 결과 캐싱, 중복 요청 방지 |
| 프론트엔드 | React + Leaflet.js | OSM 지도 렌더링, 경로 표시 |
| 배포 | Docker Compose | 로컬 및 서버 동일 환경 |

> 결정 전이므로 팀 또는 개인 상황에 따라 변경 가능.

---

## 3. 데이터 수집 (data.go.kr 연동)

### 3-1. 활용 API 목록
| API | 제공처 | 내용 |
|-----|--------|------|
| 고속버스 운행정보 조회 | 국토교통부 | 고속버스 노선·시간표 |
| 시외버스 운행정보 조회 | 국토교통부 | 시외버스 노선·시간표 |
| 철도 운행정보 조회 | 한국철도공사(KORAIL) | 열차 운행 시간표 |
| 버스터미널 정보 조회 | 국토교통부 | 터미널 위치, 명칭 |
| 기차역 정보 조회 | 한국철도공사 | 역 위치, 코드 |

### 3-2. 폴링 전략
- 시간표 데이터는 하루 1회 새벽에 전량 갱신 (daily full refresh)
- 실시간 운행 지연 데이터가 제공될 경우 15~30분 간격으로 증분 갱신
- API 키는 환경변수(`DATAGOKR_API_KEY`)로 주입, 코드에 포함 금지
- 폴링 실패 시 재시도 로직 (exponential backoff) 적용

### 3-3. 저장 구조 (논리 모델)
```
Terminal / Station
  - id, name, type(bus|rail), lat, lon, region_code

Route
  - id, type(express|intercity|rail), origin_id, destination_id

Schedule
  - id, route_id, departure_time, arrival_time, operator, days_of_week
```

---

## 4. 지도 시각화 (OSM + Leaflet)

- 전국 터미널/역 마커 표시
- 검색 결과 경로를 폴리라인으로 표시 (출발→환승→도착)
- 클릭 시 해당 정류장 발착 시간표 팝업
- 타일 서버: OpenStreetMap 기본 타일 (또는 자체 tile proxy 캐싱)
- 모바일 반응형 레이아웃

---

## 5. 경로 검색 로직

### 5-1. 직행 검색
- 출발 터미널 → 도착 터미널 직행 스케줄 조회
- 당일/익일 출발 시간 기준 필터링

### 5-2. 환승 검색 (핵심 기능)
```
예) 부산 → 청주
  1단계: 부산 → [중간거점] 직행 노선 탐색
  2단계: [중간거점] → 청주 직행 노선 탐색
  환승 대기 시간 30분 이상인 조합만 유효로 처리
```
- BFS/Dijkstra 기반 최소 환승 또는 최소 시간 경로 탐색
- 최대 환승 횟수: 2회 (1회 환승까지 우선 구현, 2회는 선택)
- 교통수단 혼합 허용 여부를 옵션으로 제공 (버스 전용 / 철도 포함)

### 5-3. 검색 결과 정렬 기준
1. 총 소요 시간
2. 출발 시각
3. 환승 횟수

---

## 6. API 설계 (백엔드)

| Method | Path | 설명 |
|--------|------|------|
| GET | `/api/terminals` | 전체 터미널/역 목록 |
| GET | `/api/terminals/:id` | 특정 터미널 정보 |
| GET | `/api/search` | 경로 검색 (`from`, `to`, `date`, `mode` 파라미터) |
| GET | `/api/schedule/:route_id` | 특정 노선 시간표 |
| GET | `/api/regions` | 권역(지역) 목록 |

---

## 7. 개발 단계

### Phase 1 — 데이터 파이프라인 (MVP)
- [ ] data.go.kr API 키 발급 및 연동 테스트
- [ ] 터미널/역 데이터 수집 및 DB 저장
- [ ] 고속버스 시간표 수집 및 저장
- [ ] 시외버스 시간표 수집 및 저장
- [ ] 철도 시간표 수집 및 저장
- [ ] 일일 폴링 스케줄러 구현

### Phase 2 — 검색 API
- [ ] 직행 경로 검색 API 구현
- [ ] 1회 환승 경로 탐색 구현
- [ ] 검색 결과 정렬 및 페이지네이션

### Phase 3 — 프론트엔드 & 지도
- [ ] Leaflet + OSM 기반 지도 뷰 구현
- [ ] 터미널/역 마커 표시
- [ ] 경로 검색 UI (출발지·도착지 입력)
- [ ] 검색 결과 리스트 및 지도 폴리라인 표시

### Phase 4 — 고도화
- [ ] 2회 환승 탐색
- [ ] 버스+철도 혼합 경로
- [ ] 즐겨찾기 / 최근 검색
- [ ] 모바일 최적화

---

## 8. 미결 사항 및 결정 필요 항목

- [ ] 백엔드 언어 최종 결정 (Go / Python / Node.js)
- [ ] 프론트엔드 SPA 여부 (React / Vue / 서버사이드 렌더링)
- [ ] 환승 탐색 시 버스↔철도 혼합 허용 여부
- [ ] 배포 환경 (자체 서버 / 클라우드 / Vercel+Supabase 등)
- [ ] data.go.kr API 호출 제한 확인 및 캐싱 정책 확정
